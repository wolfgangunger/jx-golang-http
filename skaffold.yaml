apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/wolfgangunger/jx-golang-http:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/wolfgangunger/jx-golang-http:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: jx-golang-http
        chartPath: charts/jx-golang-http
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/wolfgangunger/jx-golang-http"
          image.tag: "{{.DIGEST_HEX}}"
